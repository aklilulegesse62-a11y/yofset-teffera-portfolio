import type{Metadata}from"next";import{siteContent}from"../data";
export const metadata:Metadata={title:"About"};
export default function About(){return <main id="main"><header className="page-hero wrap"><p className="eyebrow">Artist biography</p><h1>About <em>Yofset</em></h1></header><div className="portrait-placeholder wrap"><span>Artist portrait forthcoming</span></div><section className="bio-grid wrap"><h2>A practice shaped by <em>heritage</em> and feeling.</h2><div className="prose">{siteContent.biography.map(p=><p key={p}>{p}</p>)}<div className="facts">{siteContent.facts.map(f=><span key={f}>{f}</span>)}</div></div></section><section id="statement" className="statement-full navy"><div className="wrap"><p className="section-no">Artist Statement</p><div className="prose">{siteContent.statement.map(p=><p key={p}>{p}</p>)}</div></div></section></main>}
