import type{Metadata}from"next";import{siteContent}from"../data";
export const metadata:Metadata={title:"Exhibitions"};
export default function Exhibitions(){return <main id="main"><header className="page-hero wrap"><p className="eyebrow">Solo exhibitions</p><h1>Chapters in <em>motion</em></h1><p>Each exhibition records an evolving conversation between identity, emotion and place.</p></header><section className="exhibitions-list wrap">{siteContent.exhibitions.map((e,i)=><article className="exhibition" key={e.title}><span className="ex-number">0{i+1}</span><div><h2>{e.amharic}<small>{e.title}</small></h2><div className="ex-meta"><p><b>Date / Status</b><br/>{e.date}</p><p><b>Venue</b><br/>{e.venue}</p><p><b>Location</b><br/>{e.location}</p></div><p className="ex-desc">{e.description}</p>{e.context&&<p>{e.context}</p>}</div></article>)}</section></main>}
