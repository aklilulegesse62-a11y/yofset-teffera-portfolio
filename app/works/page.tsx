import type {Metadata} from "next";import {siteContent} from "../data";import {WorksGallery} from "../ui/WorksGallery";
export const metadata:Metadata={title:"Selected Works"};
export default function Works(){return <main id="main" className="works-page"><header className="page-hero works-hero wrap"><div><p className="eyebrow">The portfolio · Addis Ababa</p><h1>Selected <em>Works</em></h1></div><p>Painting, memory and contemporary Ethiopian visual storytelling. Browse the collection by practice.</p></header>{siteContent.artworks.length?<WorksGallery artworks={siteContent.artworks}/>:<div className="empty-art wrap"><div className="empty-circle" aria-hidden="true"/><p>Artwork collection forthcoming.<br/>No supplied artwork files were available yet.</p></div>}</main>}
