import type {Metadata} from "next";import {siteContent} from "../data";import {ArtworkCard} from "../ui/ArtworkCard";
export const metadata:Metadata={title:"Selected Works"};
export default function Works(){return <main id="main" className="works-page"><header className="page-hero wrap"><p className="eyebrow">The portfolio</p><h1>Selected <em>Works</em></h1><p>Painting, memory and contemporary Ethiopian visual storytelling.</p></header><div className="wrap filter-row" aria-label="Artwork categories"><span>Selected Works</span><span>Paintings</span><span>Mixed Media</span><span>Objects</span><span>Spatial Work</span></div>{siteContent.artworks.length?<div className="art-grid wrap">{siteContent.artworks.map(a=><ArtworkCard key={a.slug} artwork={a}/>)}</div>:<div className="empty-art wrap"><div className="empty-circle" aria-hidden="true"/><p>Artwork collection forthcoming.<br/>No supplied artwork files were available yet.</p></div>}</main>}
