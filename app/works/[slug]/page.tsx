import Image from "next/image";import Link from "next/link";import {notFound} from "next/navigation";import {siteContent} from "../../data";
export function generateStaticParams(){return siteContent.artworks.map(a=>({slug:a.slug}))}
export default async function ArtworkDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const i=siteContent.artworks.findIndex(a=>a.slug===slug);if(i<0)notFound();const a=siteContent.artworks[i];const prev=siteContent.artworks[(i-1+siteContent.artworks.length)%siteContent.artworks.length];const next=siteContent.artworks[(i+1)%siteContent.artworks.length];return <main id="main" className="wrap page-hero"><Link className="text-link" href="/works">← Back to Selected Works</Link><div className="bio-grid"><Image src={a.image} alt={a.alt} width={1600} height={2000} sizes="(max-width:800px) 100vw, 55vw"/><div><p className="eyebrow">{a.category}</p><h1>{a.title}</h1><div className="contact-meta"><div><small>Year</small>{a.year}</div><div><small>Medium</small>{a.medium}</div><div><small>Dimensions</small>{a.dimensions}</div></div><p className="large-copy">{a.description}</p></div></div><div className="footer-bottom"><Link href={`/works/${prev.slug}`}>← Previous: {prev.title}</Link><Link href={`/works/${next.slug}`}>Next: {next.title} →</Link></div></main>}
